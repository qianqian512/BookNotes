
#### 跟DBA聊了一会
每日优鲜：MySQL数据主备同步机制，5.6版本主备是半同步机制，不能保证数据完全一致，极端情况下数据在同步中发生主备切换，主库半状态数据会丢失。而5.7是强一致性的，采用主备同步写机制，任何情况下发生主备切换时，都能保证主备数据的一致性
#### MySQL主备架构
masterVIP -> proxyCluster -> MasterIP
			      |
				  +--------> BackupIP