
### 什么情况下会出现linux物理内存还有很多，但交换空间也使用了很多？
例如：一个占用内存很大的进程启动时，linux会为这个进程分配很多物理内存，而与此同时一些不常用的内存会被换出到磁盘中。但后来这个占用很多内存的进程结束了，将占用的物理内存释放掉了，
但被交换出去的内存并不会马上回到物理内存（只有在用到的时候才会再从磁盘中换入到内存），此时linux系统中就会出现物理内存很多，但交换空间被用掉了也很多的情况。
