存储结构	
数组+链表+红黑树
Node节点：

并发Map的负载能修改吗？
不能，0.75是final修改的

Node.hash的值为什么要必须大于等于0？

sizeCtl的含义
-1：散列表正在初始化，确保只能被创建一次。
大于0：阈值
小于-1：扩容状态，高16位标识扩容状态，低16位表示参与扩容工作的线程数+1

扩容表示戳计算方式？

cmap如何保证写数据安全

hash寻址算法

CAS高并发时性能为什么低效？
高并发下，大部分线程都是陪跑性质，只能有一个线程拿锁成功。

触发扩容的线程，都做了哪些额外的预处理工作

假如put操作时正好有别的线程正在对table数组(map)扩容怎么办
暂停put操作，先帮助其他线程对map扩容。

cmap如何统计散列表的数据量，为什么不用AtomicLong
使用LongAddr，

cmap如何在并发的情况下初始化散列表

