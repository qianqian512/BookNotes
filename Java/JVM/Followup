栈中包含了帧栈，帧栈中存了什么？（4类信息：局部变量，操作数栈，动态连接，方法出口）
jmm分为哪几部分
对象头都存了什么
eden、s0和s1协作过程
jvm调优的目的是什么？（减少gc次数，一般线上younggc和fullgc的频率和次数是多少）
如何分配线上的jvm参数，需要从哪几方面考虑，新生代如果设置过大有什么问题吗？
触发fullgc条件
对象从新生代晋升到老年代有哪些要求和条件 

Java中的零拷贝和Netty的零拷贝，以及操作系统的零拷贝区别
如何使用interrupted？
什么情况下 会造成ygc ,ogc ,fgc?

指针碰撞和TLAB的关系
  1) 指针碰撞时JVM在分配内存时的一种方式，当整个内存区域没有碎片时（例如基于标记复制算法GC），通过在可用内存上移动指针来分配分配内存。
  2) TLAB是为了解决指针碰撞的并发问题，因为不同线程共用JVM堆空间，因此在并发分配内存时会有抢热点数据问题，而TLAB解决方案就是为每个线程预分配一段内存空间，来将内存分片来分散热点数据。
标量替换 栈上分配 同步消除